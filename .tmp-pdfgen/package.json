{
  "name": "partner-pdfgen",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix ../frontend install && npm --prefix ../frontend run build && rm -rf .next && cp -R ../frontend/.next ./.next",
    "start": "npm --prefix ../frontend run start"
  },
  "dependencies": {
    "pdfkit": "^0.18.0"
  }
}
